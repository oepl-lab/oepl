{
  "name": "oepl",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix oepl-web",
    "build": "npm run build --prefix oepl-web",
    "start": "npm run start --prefix oepl-web",
    "lint": "npm run lint --prefix oepl-web"
  }
}
